[project]
name = "cognism-sciops-postprocessor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.1",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["cognism_sciops.postprocessor*"]
